function validTimeZone(value){try{new Intl.DateTimeFormat('en',{timeZone:value}).format();return true;}catch{return false;}}
function zonedLocalToUtc(localIso,timeZone){if(!/^\d{4}-\d\d-\d\dT\d\d:\d\d/.test(localIso)||!validTimeZone(timeZone))throw new Error('Invalid date/time zone');const values=localIso.slice(0,16).split(/[-T:]/).map(Number),wanted=Date.UTC(values[0],values[1]-1,values[2],values[3],values[4]);let guess=wanted;for(let i=0;i<3;i++){const p=Object.fromEntries(new Intl.DateTimeFormat('en-CA',{timeZone,year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',hourCycle:'h23'}).formatToParts(new Date(guess)).filter(x=>x.type!=='literal').map(x=>[x.type,Number(x.value)]));guess+=wanted-Date.UTC(p.year,p.month-1,p.day,p.hour,p.minute);}return new Date(guess).toISOString();}module.exports={validTimeZone,zonedLocalToUtc};
