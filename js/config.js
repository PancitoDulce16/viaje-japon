// js/config.js - Application configuration
// ⚠️ NOTA: Este archivo SÍ debe estar en Git
// Solo contiene configuración no sensible
// Las API keys opcionales (como Google Places) pueden estar vacías

export const APP_CONFIG = {
  // Google Places API Key (opcional) - Dejar vacío si no se usa
  // Si quieres habilitar búsqueda de lugares, añade tu key aquí
  GOOGLE_PLACES_API_KEY: ''
};
